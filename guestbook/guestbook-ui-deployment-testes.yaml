apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui-testes
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-ui-testes
  template:
    metadata:
      labels:
        app: guestbook-ui-testes
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-ui-testes
        ports:
        - containerPort: 80
